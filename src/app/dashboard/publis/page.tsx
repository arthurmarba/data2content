"use client";

import PublisClient from './PublisClient';

export default function PublisPage() {
    return (
        <div className="dashboard-page-shell">
            <PublisClient />
        </div>
    );
}
